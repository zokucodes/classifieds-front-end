import { useLocation } from "react-router-dom";

const PageManageListing = () => {
    const location = useLocation();
    const queryParams = new URLSearchParams(location.search);

    
}


export default PageManageListing